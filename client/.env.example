# WebSocket URL of the collaboration server.
# Local dev:
VITE_WS_URL=ws://localhost:1234
# Production (Render): wss://your-service.onrender.com

# Code-execution server for the Run button (non-JS languages).
# JavaScript runs in-browser and ignores this.
# Self-hosted Piston (see docker-compose.yml):
# VITE_PISTON_URL=http://localhost:2000/api/v2
